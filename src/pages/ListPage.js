import React from 'react'

const ListPage = () => {
    const title = (() => {
        let title = 'gahusb\'s blog';
        return title
    })();

    return (
        <title>{title}</title>
    );
};

export default ListPage;